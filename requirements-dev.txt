# Development and CI dependencies.
#
# The engine itself needs nothing but the standard library - that is the point.
# Everything here is for testing, linting and the replay script.

pytest>=8.0
pytest-cov>=5.0
pyyaml>=6.0

# Home Assistant runtime tests (tests/hass/). POSIX only: the plugin needs
# fcntl and unix sockets, so on Windows run `pytest -p no:homeassistant`.
pytest-homeassistant-custom-component>=0.13

ruff>=0.6
mypy>=1.11
